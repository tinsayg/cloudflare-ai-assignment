{
  "name": "cloudflare-ai-assignment",
  "version": "1.0.0",
  "description": "AI-powered chat application on Cloudflare with Llama 3.3, Workflows, and real-time features",
  "main": "src/worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:staging": "wrangler deploy --env staging",
    "deploy:prod": "wrangler deploy --env production",
    "build:pages": "cd pages && npm run build",
    "dev:pages": "cd pages && npm run dev"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.78.12"
  },
  "dependencies": {
    "@cloudflare/ai": "^1.0.57"
  }
}
